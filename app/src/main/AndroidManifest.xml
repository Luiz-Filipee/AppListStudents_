<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".AgendaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_list_background_final"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_list_background_final"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="33">
        <activity
            android:name=".ui.activity.LoginAlunosActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ListaAlunosActivity"
            android:exported="true"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.activity.FormularioAlunoActivity"
            android:exported="true" />
    </application>

</manifest>